# --- Dependencies & package managers ---
node_modules/
.pnpm-store/

# --- Logs & debug ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- Environment & secrets (never commit) ---
.env
.env.*
!.env.example

# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride
# Volume custom icon (name is "Icon" + carriage return)
Icon?
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# --- Editors / IDEs ---
.idea/
*.iml
*.swp
*.swo
*~

# --- TypeScript / tooling caches ---
*.tsbuildinfo
.cache/
.turbo/
.eslintcache
.stylelintcache

# --- Test / coverage (if added later) ---
coverage/
.nyc_output/

# --- Front (Vite) build output ---
web/dist/

# --- Optional: local Chromium/Puppeteer override dirs (if you set PUPPETEER_CACHE_DIR in-repo) ---
# .puppeteer-cache/
